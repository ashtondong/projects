#include <cs50.h>
#include <stdio.h>
#include <string.h>
#include <math.h>

//creating and appending an array
int main(void)
{
    int arr[8] = {0}; // here all elements are initialized to 0
    string word = "HI";
    int len = strlen(word);

    for (int i = 0; i < 8; i++)
    {
        arr[i] = i;
        printf("%i", arr[i]);



    }



}


for (int j = (word[i]); j > 0; j = j/2)
        {

            if (j % 2 == 0)
            {
                bit = 0;
                arr[i] = bit;



            }
            else
            {
                bit = 1;
                arr[i] = bit;



            }

        }





